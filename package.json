{
  "name": "wolf",
  "version": "1.0.1",
  "main": "index.js",
  "author": "eric",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "build:builder": "cd ./packages/wolf-builder && npm run build",
    "build:cli": "cd ./packages/wolf-cli && npm run build",
    "build:cli-nodejs": "cd ./packages/wolf-cli-nodejs && npm run build",
    "build:cli-web": "cd ./packages/wolf-cli-web && npm run build",
    "build:utils": "cd ./packages/wolf-utils && npm run build",
    "build:init": "cd ./packages/wolf-init && npm run build",
    "build": "npm run build:builder && npm run build:cli && npm run build:cli-nodejs && npm run build:cli-web && npm run build:utils && npm run build:init"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.12",
    "babel": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "cross-env": "^5.2.0",
    "lerna": "^3.5.1",
    "typescript": "^3.2.1",
    "webpack": "^4.27.1",
    "webpack-cli": "^3.1.2",
    "webpack-dev-server": "^3.1.10"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "6.10"
          }
        }
      ]
    ]
  }
}
